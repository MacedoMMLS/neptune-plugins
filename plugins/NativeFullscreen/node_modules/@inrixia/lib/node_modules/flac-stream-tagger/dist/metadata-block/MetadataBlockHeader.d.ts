import { BufferBase } from "../lib/BufferBase.js";
export declare enum MetadataBlockType {
    StreamInfo = 0,
    Padding = 1,
    Application = 2,
    SeekTable = 3,
    VorbisComment = 4,
    CueSheet = 5,
    Picture = 6,
    Invalid = 127
}
export declare const MetadataBlockHeaderLength = 4;
export declare class MetadataBlockHeader<T extends MetadataBlockType> extends BufferBase {
    type: T;
    dataLength: number;
    isLast: boolean;
    constructor(type: T, dataLength?: number, isLast?: boolean);
    static readonly SIZE = 4;
    static fromBuffer<T extends MetadataBlockType>(buffer: Buffer): MetadataBlockHeader<T>;
    toBuffer(): Buffer;
    get length(): number;
}
