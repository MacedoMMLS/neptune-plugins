import util from "util";
/**
 * Inspects a object
 * @param obj Object to inspect
 * @param options.showHidden Show hidden properties of object
 * @param options.depth Depth to inspect object
 * @param options.color Color resulting output
 */
export const iObj = (obj, ...options) => {
    if (options[0] === undefined || options[0] === null)
        options[0] = false;
    if (options[1] === undefined || options[1] === null)
        options[1] = 5;
    if (options[2] === undefined || options[2] === null)
        options[2] = true;
    return util.inspect(obj, ...options);
};
/**
 * Logs a inspected object to console
 * @param obj Object to inspect
 * @param options.showHidden Show hidden properties of object
 * @param options.depth Depth to inspect object
 * @param options.color Color resulting output
 */
export const lObj = (obj, ...options) => console.log(iObj(obj, ...options));
/**
 * Single line inspect object.
 * @param obj Object to inspect
 * @param options.showHidden Show hidden properties of object
 * @param options.depth Depth to inspect object
 * @param options.color Color resulting output
 */
export const sliObj = (obj, ...options) => iObj(obj, ...options)
    .replace(/\r?\n/g, " ")
    .replace(/  +/g, " ");
//# sourceMappingURL=inspect.js.map