export declare abstract class BufferBase {
    abstract toBuffer(): Buffer;
    abstract get length(): number;
}
export declare const allocBufferAndWrite: (size: number, onWrite: (buffer: Buffer) => void) => Buffer;
